version: 2
build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  jobs:
    install:
      - pip install uv
      - uv sync --group docs
    pre_build:
      - uv run python tools/build_docs_wheel.py
sphinx:
  configuration: docs/conf.py
