Beyond the chain
================

What surrounds the builder: composing patterns from parts, running them, reading
what they do, storing them, and wiring them into a framework.

.. toctree::
   :maxdepth: 1

   composing
   from-regex
   matching
   errors
   testing
   seeing
   serialization
   integrations

- :doc:`composing` — reuse, operators, constants, and factory functions.
- :doc:`from-regex` — turning an existing regex string back into a chain.
- :doc:`matching` — running a compiled pattern against text.
- :doc:`errors` — the four-part diagnostics edify raises.
- :doc:`testing` — inline assertions and snapshot tests.
- :doc:`seeing` — explaining a pattern in prose or as a diagram.
- :doc:`serialization` — round-tripping a pattern through JSON.
- :doc:`integrations` — using a pattern as a framework validator.
